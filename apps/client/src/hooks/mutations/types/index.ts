/**
 * React Query mutation hook에서 사용할 옵션 타입입니다.
 * @property showToast - 성공 시 토스트를 띄울지 여부 (기본값: true)
 * // 앞으로 옵션이 추가될 경우 여기에 작성
 */
export type UseUpdateCartItemOptions = {
    showToast?: boolean;
};
